# Trajectory Type Web Prototype

Static browser prototype for drawing camera-sampled strokes with pointer input.

Run locally:

```sh
python3 -m http.server 5174 --directory web
```

Open `http://127.0.0.1:5174/`. Camera access works on localhost or HTTPS origins.
